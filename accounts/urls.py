# accounts/urls.py
from django.urls import path
from django.contrib.auth import views as auth_views
from . import views

urlpatterns = [
    # Add account-specific URLs here if needed
    # For now, we'll use Django's built-in auth URLs
]